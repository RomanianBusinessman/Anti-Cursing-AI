# Anti-Cursing-AI
Removes cursing from a video using FFMPEG and Faster Whisper. Currently is a simple python script.
The model itself is in the model folder. It loads curse words to remove from the curses.txt file.
To use it, run the model.py file in your terminal and input the name of your video (MP4 Recommended)
